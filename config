#!/bin/bash

LIBUV_BRANCH=v1.11.0
CORECLR_BRANCH=master
COREFX_BRANCH=master
ROSLYN_BRANCH=version-1.3.2
COREHOST_BRANCH=master
CLI_BRANCH=master

ROOTFS_BRANCH=xenial
ROOTFS_DIR=$(pwd)/rootfs/$ROOTFS_BRANCH
ROOTFS_INSTALL_DIR=$ROOTFS_DIR/dotnet

OS_NAME=Linux
OS_DIST=ubuntu
OS_VERSION=16.04

ARCH=arm
BUILD_CONFIG=Release
RUNTIME_ID=$OS_DIST.$OS_VERSION-$ARCH
